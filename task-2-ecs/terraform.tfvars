# Non-sensitive variable values for this deployment.
# Sensitive values (app_name, db_host, api_key, db_password) come from TF_VAR_* in ~/.bashrc.
aws_region   = "eu-central-1"
project_name = "ecs-task-app"
owner        = "mukesh"
